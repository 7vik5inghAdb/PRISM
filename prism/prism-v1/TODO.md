# PRISM v1 — Parked Decisions

Running list of decisions deferred during milestone work. Resolve before the milestone that depends on each item.

## Open

- [ ] **System prompt for `/api/chat`** — the chat route currently streams from Claude with no system prompt. Decide what (if anything) the assistant should say or be by default. Likely resolved when M6 (orchestrator) lands and the real orchestrator system prompt is wired. _Parked: M3._
